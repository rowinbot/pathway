---
interface Props {
  href: string;
  target?: string;
  rel?: string;
  size?: "sm" | "base";
  variant?: "underline" | "base";
}

const {
  href,
  target = "_self",
  rel,
  size = "base",
  variant = "base",
} = Astro.props;
---

<a
  class:list={[
    "transition-all duration-150 rounded-lg uppercase",
    size === "sm" && "px-3 py-2 text-sm",
    size === "base" && "px-4 py-2 text-base",
    variant === "underline" &&
      "border-[1px] hover:border-gray-500 border-gray-transparent",
    variant === "base" && "bg-black text-white border-[1px] border-black",
  ]}
  href={href}
  target={target}
  rel={rel}><slot /></a
>
